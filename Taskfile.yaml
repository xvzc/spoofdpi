version: '3'

tasks:
  default:
    desc: List available tasks
    cmd: task --list

  build:
    desc: Build spoofdpi binary
    cmd: go build ./cmd/...

  run:
    desc: Build and run with dynamic arguments
    deps: [build]
    cmd: sudo -E ./spoofdpi {{.CLI_ARGS}}

  test:
    desc: Run tests with race detector
    cmd: go test {{.CLI_ARGS}} -race -tags network ./...

  lint:
    desc: Run golangci-lint
    cmd: golangci-lint run

  fmt:
    desc: Format code with golangci-lint
    cmd: golangci-lint fmt

  fmt-check:
    desc: Verify formatting matches golangci-lint
    cmd: golangci-lint fmt --diff

  pre-commit:
    desc: Pre-commit checks (test + fmt-check + lint)
    cmds:
      - task: test
      - task: fmt-check
      - task: lint

  docs:
    desc: Serve docs locally (mkdocs)
    cmd: mkdocs serve
